ALL PHASES COMPLETE — 2026-03-02

Phase 1: Auth (register/invite, email verify, login httpOnly JWT, logout, refresh rotation, forgot/reset), Invitation system, Family CRUD + join requests, Persons (propose-add), zero TS errors
Phase 2: Canvas-based tree visualization (pan/zoom/click), Relationships API (propose, approve, traverse), Indian/International label toggle, GET /families/:id/tree endpoint
Phase 3: Notifications API (list, mark-read, mark-all-read), Audit log fully wired, Admin panel with proposals + join request management
Phase 4: Super admin dashboard (families overview, audit log, promote users, soft-delete families), Tree merging (merge requests + dual-admin approval flow)
Phase 5: .gitignore added, node_modules removed from git, clean repo

To run:
1. cd server && npm install && npm run db:migrate && npm run dev
2. cd client && npm install && npm run dev
3. Visit http://localhost:5173
